const myName = "Max";

if (myName === "Max") {
  console.log("Hello!");
}

const isLoggedIn = true;

if (isLoggedIn) {
  console.log("User is logged in");
} else if (!isLoggedIn) {
  console.log("User is NOT logged in");
}

const enteredUserName = "Josema"; //empty sting or 0 are treated as false

if (enteredUserName) {
  console.log("Input is valid!");
}
